; StrayDLSS — configuration. 0.0.1 (the attach-timing probe) reads nothing from here yet;
; the file exists so the install layout is final from the first build. Later builds carry
; every [STRAYDLSS] key ReShade.ini carries today, hot-reloaded.

[STRAYDLSS]
