# Build
build/
out/
cmake-build-*/
*.obj
*.pdb
*.ilk
*.exp
*.lib
*.dll
*.addon
*.addon64

# Vendored binaries — never commit
nvngx_dlss.dll
third_party/dlss/lib/**/*.dll

# Reference checkouts (kept outside the repo; this is a safety net)
refs/
.vs/
.idea/
.DS_Store
build-local/
build*/
third_party/ngx/lib/

# Python bytecode caches (one was committed by accident in 517f3fb).
__pycache__/
*.pyc

# The DLSSNR runtime (165 MB, leaked pre-release, never redistributable). Kept at the repo
# root so binary investigation needs no box round trip; md5 eea91faf55a8993656c66815f0497b3b,
# byte-identical to what the game loads.
/nvngx_dlssnr.dll
